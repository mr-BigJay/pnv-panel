{
  "name": "pnv-panel",
  "user": "ubuntu",
  "install": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y php-cli php-sqlite3 php-gd php-mbstring php-curl && mkdir -p temp phpqrcode/cache",
  "terminals": [
    {
      "name": "PHP Server",
      "command": "php -S 0.0.0.0:8080 -t ."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8080
    }
  ]
}
